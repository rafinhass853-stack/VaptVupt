rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DESENVOLVIMENTO: liberar tudo
    // ⚠️ TROCAR ANTES DE PRODUÇÃO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}