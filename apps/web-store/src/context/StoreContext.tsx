import {createContext,useContext,useEffect,useState} from "react";import type{ReactNode}from"react";import{onAuthStateChanged,signInWithEmailAndPassword,signOut}from"firebase/auth";import type{User}from"firebase/auth";import{collection,query,where,getDocs}from"firebase/firestore";import{auth,db}from"../lib/firebase";
export interface StoreData{id:string;name:string;slug:string;balance:number;uid:string;address:{street:string;number:string;lat:number;lng:number};}
interface StoreContextType{user:User|null;store:StoreData|null;loading:boolean;login:(email:string,password:string)=>Promise<void>;logout:()=>Promise<void>;refreshStore:()=>Promise<void>;}
const StoreContext=createContext<StoreContextType|undefined>(undefined);
export function StoreProvider({children}:{children:ReactNode}){const[user,setUser]=useState<User|null>(null),[store,setStore]=useState<StoreData|null>(null),[loading,setLoading]=useState(true);
const loadStore=async(u:User)=>{setStore(null);const q=query(collection(db,"stores"),where("uid","==",u.uid));const snap=await getDocs(q);if(snap.empty)throw new Error("Conta autenticada, mas nenhuma loja foi vinculada a este usuário.");const d=snap.docs[0];const data=d.data();setStore({id:d.id,name:data.name||"",slug:data.slug||"",balance:Number(data.balance||0),uid:data.uid||u.uid,address:data.address||{street:"",number:"",lat:0,lng:0}});};
useEffect(()=>{return onAuthStateChanged(auth,async u=>{setUser(u);if(u){try{await loadStore(u)}catch(e){console.error(e);setStore(null)}}else setStore(null);setLoading(false)})},[]);
const login=async(email:string,password:string)=>{await signInWithEmailAndPassword(auth,email,password)};const logout=async()=>{await signOut(auth);setStore(null)};const refreshStore=async()=>{if(user)await loadStore(user)};return <StoreContext.Provider value={{user,store,loading,login,logout,refreshStore}}>{children}</StoreContext.Provider>}
export function useStore(){const c=useContext(StoreContext);if(!c)throw new Error("useStore deve ser usado dentro de StoreProvider");return c}